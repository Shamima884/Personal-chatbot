# The core web app uses only the Python standard library (http.server + urllib).
# Optional: PostgreSQL driver for persisting chat history (see README).
#   pip install -r requirements.txt
psycopg[binary]>=3.1